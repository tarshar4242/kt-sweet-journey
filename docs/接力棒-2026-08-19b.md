# 接力棒｜2026-08-19（第二棒，給下一個 Claude）

> 給接手的你：先讀這頁就能無縫接上。Tarshar 在做「課程筆記館」(kt-sweet-journey)。
> 這一棒原本卡著一個未完成任務（skills.html 盤點），**已於 2026-08-20 完成上線**，
> 處理結果見下方「✅ 已完成」，其餘各件本來就已上線。

---

## ✅ 已完成（2026-08-20）→ 原「你要接的任務」：skills.html 技能盤點

**結果：** `add_repo tarshar4242/thesis-library` 經 Tarshar 核准後讀到 `skills.html` 原始碼，
盤點頁內 JS `SKILLS` 陣列（原 46 張卡）對上實際擁有的技能，缺的正好是 kt-sweet-journey
`.claude/skills/` 裡版控的 7 個，先列清單給她確認後才補（46 → 53）：

- 品牌線＋4：`proposal-strategist`、`essay-structure-coach`、`lecture-notes-site`、`research-digest`
- 制度與日常＋3：`create-ai-collaboration-journal`、`ai-coevolution-journal`、`i-have-adhd`

同步做的：`skills-files/` 收錄 7 份 SKILL.md 原文、`manifest.json` 與 zip 更新為 49 檔、
頁面「下載全部」數字改 49、本 repo `lessons.html`／`index.html` 入口卡改「7 線 · 53 技能」
（原本描述寫 45、徽章寫 46，兩邊不一致，一併修正）。
另經她拍板，把「教案圖卡總管」的呼叫代碼從舊名 `tar-lesson-card-manager` 更名為現行正式名稱
`lesson-card-organizer`（檔名、frontmatter、manifest、zip 全部同步）。

⚠️ 給下一棒：`skills.html` 正本在 **`tarshar4242/thesis-library`**，不在本 repo；
以後技能有增減，要同時改那邊的 `SKILLS` 陣列＋`skills-files/`，和本 repo 入口卡的技能數字。

<details>
<summary>原始任務說明（保留備查）</summary>

**Tarshar 想要什麼：**
課程館首頁有個系列「🛠️ SKILL 技能包」，連到 `https://tarshar4242.github.io/thesis-library/skills.html`
（那是一份「Claude 完整能力目錄」，卡片說 7 大主線 45~46 個技能，頁內另寫「可下載 42 個」）。
她要**盤點兩邊**：把「現在實際有的技能」對上「skills.html 已收錄的技能」，
找出**現已有、但還沒補進 skills.html 的技能**，**確認後再補**（她強調先盤點、確認，才動手加）。

**卡在哪（重要）：**
- `skills.html` 不在本 repo，而在 **`tarshar4242/thesis-library`** repo。
- 本 session 的 repo 範圍只有 `tarshar4242/kt-sweet-journey`，
  `get_file_contents` 讀 thesis-library **被拒**，`add_repo` **需要 Tarshar 核准**。
- skills.html 是 **JS 動態渲染**（資料在頁內 JS 的陣列裡，像 lessons.html 的 DATA），
  WebFetch 只會看到空殼（total 0），**一定要讀原始碼**才盤得到。

**接手怎麼做：**
1. 請 Tarshar 核准 `add_repo tarshar4242/thesis-library`（或直接在 thesis-library 那個 repo 開 session）。
2. 讀 skills.html 原始碼，抽出頁內 JS 陣列裡「已收錄的技能名單」。
3. 對照「現在實際有的技能」——來源是本次 session reminder 的技能清單／她 vault 的 `skills/`。
   ⚠️ 注意排除 Anthropic 內建通用技能（docx/pdf/pptx/xlsx/code-review/design/dataviz/artifact-* 等），
   skills.html 收的是**她個人的 Tarshar 專屬技能**。
4. 列出「有、但沒收錄」的清單 → **先給她確認，不要直接加**。
5. 確認後再補進 skills.html 的 JS 陣列（照既有卡片格式），commit + 上線。

</details>

---

## ✅ 前一個 session 已完成（都 commit + 合併 main + GitHub Pages 上線）

1. **S3E3 逐幀筆記**（益師傅訂閱制第三季第三堂，2026/08/14）
   - `s3e3-notes.html`、`s3e3-ai-skills.html`、`s3e3-img/`（原名 EP50，已改）
   - 主題：聲刻本機聲音克隆 × 貼課通課堂即時牆 × 用 Google Sites 把單檔工具上雲

2. **課程館新增「📦 訂閱制課程」子系列**
   - lessons.html（＋index.html）：`showSeries('sub')`＋`#view-sub`＋`SERIES_META.sub`＋`TOC_SERIES` 一筆；首頁系列卡 SERIES 編號順移
   - ⚠️ 這堂不在直播電台 EP 編號裡，別當 EP50；回入口錨點 `lessons.html#sub`

3. **★ 逐幀筆記密度規則（A 抽格＋B 輸出，預設精簡）**
   - 正本 `.claude/skills/lecture-notes-site/SKILL.md`；觸發層 `CLAUDE.md`
   - A：依影片總長選抽格，總格數壓 ~200（≤50分15秒/50–90分25秒/>90分35秒/>150分40秒或分集）
   - B：預設精簡（筆記 8–10 節、一個工具一節）；她說「完整版/詳細版」才做 15 節
   - 另有純文字 `逐幀筆記密度規則.txt` 已交她（待她自己放進 vault）

4. **AI協作日誌03**：`docs/AI協作日誌03-課程筆記館.html`（九部結構，記今天的協作與規則改版）
5. **第一棒**：`docs/接力棒-課程筆記館-2026-08-19.md`（今天改動總表）

---

## 現在狀態
- 工作分支：`claude/course-materials-frame-notes-uly8q5`，已與 main 同步。
- 上面 1–5 全部上線；skills.html 盤點也已於 2026-08-20 完成（見本頁最上方），
  兩邊改動走分支 `claude/skill-page-missing-items-orsosw` 合併回 main（kt-sweet-journey 與 thesis-library 各一）。
- 目前沒有未完成的接手任務。

## 待 Tarshar 決定 / 提醒
- vault 同步（密度規則 .txt）只能她手動做，雲端搆不到 tarshar-vault。
- 逐幀鐵則不變：資料來源不可跳過/降級，要真的每格看完。
- 對外結構（系列命名/編號）一律回她拍板。
