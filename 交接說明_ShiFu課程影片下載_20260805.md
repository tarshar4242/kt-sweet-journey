# 交接說明｜下載 ShiFu 付費課程影片（yt-dlp 除錯進度）

## 1. 這次的目標

妳想把自己在 ShiFu（shifu.tw）付費訂閱的線上課程影片抓下來，做成只給自己看的逐幀課堂筆記。因為這個平台的影片要登入才能看，沒辦法用一般下載工具直接抓，所以我們改用一個免費小工具 yt-dlp（一個可以下載網路影片的指令列程式）配合妳瀏覽器的登入狀態來抓。**目前進度：第一堂課（免費的「01 發刊詞」）已經成功完整下載到妳的 Mac 上**，下一步是上傳到 Google Drive 交給我做逐幀筆記。

## 2. 我做了哪些事

- 先跟妳確認電腦是 Mac，寫了一份 Mac 專用、五個步驟的操作說明（開終端機、裝 yt-dlp、匯出登入鑰匙、下載影片、上傳 Google Drive）。
- 另外做了一份圖文版操作單（HTML 檔）傳給妳留存，內容跟文字版一致，多了 Windows 版本和常見錯誤對照表。
- 妳照著做，前三步都成功了：yt-dlp 裝好（版本號 2026.07.04）、cookies.txt（登入鑰匙檔）也正確讀到。
- 第四步用課程播放頁的網址直接下載時，出現錯誤訊息 `Unsupported URL`（意思是「這個網址格式看不懂」）。我判斷原因是這個播放頁的影片是用程式動態載入的，網頁的原始碼裡沒有藏著影片的真正位置，yt-dlp 找不到東西可以抓。
- 因此改教妳用 Chrome 瀏覽器內建的「開發者工具」（一個可以看到網頁背後在跟伺服器要什麼資料的面板）去找出影片的真實網址：打開面板、篩選 `m3u8`（一種串流影片常用的格式）、按下播放鍵幾秒、從跳出的清單裡複製那條真正的影片連結。
- 給了妳新的下載指令，把剛才複製到的真實網址帶進去，並加了 `--referer` 這個參數（用來告訴伺服器「這個請求是從 shifu.tw 網站發出的」，避免被擋）。
- 妳跑了這段指令，這次成功了：畫面顯示影像檔（85.19 MiB）和音訊檔（9.66 MiB）都下載完成，兩個檔案自動合併成一個完整的 mp4，畫面上還看到程式把合併前的兩個零件檔案刪除（這是正常的收尾動作，不是出錯）。截圖裡的網址（`customer-4m2crk6cp0b6iacf.cloudflarestream.com/...`）證實這堂課的影片是放在 Cloudflare Stream 這個影音平台上，之後其他堂課大機率也是走同一套抓法。

## 3. 為什麼這樣做

- 選 yt-dlp 而不是直接叫妳把整個播放過程錄下來，是因為錄影要花跟影片一樣長的時間（一兩個鐘頭），yt-dlp 直接抓檔案通常一分鐘內就好，比較省妳的時間。螢幕錄影是我們留著的保底方案，如果 yt-dlp 這條路真的走不通才會改用。
- 用 cookies.txt 這個做法，而不是把帳號密碼直接交給工具或交給我，是為了安全：cookies.txt 只是「暫時的登入通行證」，不是帳密本身，外流的風險比帳密小很多，而且妳可以隨時登出讓它失效。
- 第一次出現 `Unsupported URL` 的時候，我沒有建議妳換成更暴力的破解手法，而是引導妳去找「真正的影片網址」，因為這是合法、乾淨的做法，不涉及繞過平台的保護機制；如果找到的網址本身就有加密保護打不開，我們就會停下來改走螢幕錄影，不會再往下硬闖。

## 4. 檔案在哪、各是什麼用途

- 圖文版操作單：已經用檔案的方式傳給妳（檔名 `yt-dlp-course-download-guide.html`），存在妳收到訊息的地方，建議妳自己另外存一份到電腦裡，因為我這邊產生檔案的暫存資料夾不保證長期留著。
- 妳的 Mac 上，「下載」（Downloads）資料夾裡目前有：
  - `yt-dlp`：下載影片用的程式本體，之後每次下載新的課程都會用到它，不要刪除。
  - `cookies.txt`：妳的登入鑰匙檔，之後每次下載都要帶著它一起用；如果哪天登入狀態過期（下載失敗又出現要求登入的訊息），就回去 Chrome 重新匯出一份蓋掉舊的。
  - 課程影片檔（一個開頭是 `17c288ff564c666f15176faceea01c60` 的 .mp4）：已確認下載成功，在這裡，點開應該能正常播放。

## 5. 之後我可以怎麼用

- 妳只要把課程播放頁的網址開起來、用 Chrome 開發者工具照上面的方法找出真實的影片網址，就能重複用同一套指令抓下一堂課，不用每次都重新問我。
- 如果妳想偷懶，也可以每次都直接把「播放頁網址」和「開發者工具裡找到的畫面截圖」丟給我，我幫妳看是哪一條連結、指令怎麼貼。
- 抓到 mp4 之後，上傳到妳自己的 Google Drive、設定成「知道連結的使用者」可看、把連結貼給我並說一句「這是私人課程筆記」，我就會接手做逐幀筆記，不會公開上架。

## 6. 注意事項與常見問題

- 這整套方法只能用在妳自己已經付費、只給自己看的內容，不要用來抓別人帳號的課程或公開分享抓下來的影片，這樣會侵犯課程作者的權益。
- cookies.txt 是妳的登入鑰匙，不要傳給任何人（包括我），用完可以刪掉，之後要用再重新匯出。
- 如果之後下載時又出現 `Unsupported URL`，先照第 2 段的方法找真實網址；如果照做也抓不到東西，或抓下來的檔案打不開、跳出跟版權保護（DRM）有關的字樣，代表這堂課有加密保護，直接停手，改用螢幕錄影的保底方案，不要嘗試更進一步的破解工具。
- 目前只確認了第一堂（免費的那堂）能抓成功。之後抓其他堂課如果又卡在 `Unsupported URL`，代表那堂課的播放頁結構不太一樣，一樣照 Chrome 開發者工具那套方法重新找一次真實網址即可，不代表整套方法失效。
